{
  "mcpServers": {
    "sandbox-sqlcl-mcp": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "sandbox-oracle-server",
        "start-mcp"
      ]
    }
  }
}
